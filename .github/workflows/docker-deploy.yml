name: Docker Deploy (web -> K8s)

on:
  push:
    branches: [main]
    paths:
      - 'apps/web/**'
      - 'packages/**'
      - '.github/workflows/docker-deploy.yml'

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_WEB: ghcr.io/eobryandev/dev4-palpita-ai-web
  IMAGE_MIGRATOR: ghcr.io/eobryandev/dev4-palpita-ai-migrator

jobs:
  build-push:
    # ARM nativo (nodes do cluster são A1 ARM): build direto, sem QEMU.
    runs-on: ubuntu-24.04-arm
    outputs:
      sha_tag: ${{ steps.meta.outputs.sha_tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Short SHA
        id: meta
        run: echo "sha_tag=$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      - name: Login GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build + push web (runner)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/web/Dockerfile
          target: runner
          platforms: linux/arm64
          push: true
          tags: |
            ${{ env.IMAGE_WEB }}:${{ steps.meta.outputs.sha_tag }}
            ${{ env.IMAGE_WEB }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build + push migrator
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/web/Dockerfile
          target: migrator
          platforms: linux/arm64
          push: true
          tags: |
            ${{ env.IMAGE_MIGRATOR }}:${{ steps.meta.outputs.sha_tag }}
            ${{ env.IMAGE_MIGRATOR }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  bump-gitops:
    needs: build-push
    runs-on: ubuntu-latest
    steps:
      - name: Checkout infra (PAT)
        uses: actions/checkout@v4
        with:
          repository: EoBryanDev/oci-infra
          ref: main
          token: ${{ secrets.INFRA_PAT }}
          path: infra

      - name: Bump image tags
        env:
          SHA_TAG: ${{ needs.build-push.outputs.sha_tag }}
        run: |
          python3 - "$SHA_TAG" <<'EOF'
          import sys
          sha = sys.argv[1]
          p = 'infra/k8s/apps/apps/palpitai/kustomization.yaml'
          s = open(p).read()
          # Troca newTag das duas imagens (web + migrator) para o SHA do build.
          import re
          s = re.sub(r'(newName: ghcr\.io/eobryandev/dev4-palpita-ai-(?:web|migrator)\n\s+newTag: )\S+',
                     lambda m: m.group(1) + sha, s)
          open(p, 'w').write(s)
          print(open(p).read())
          EOF

      - name: Commit + push
        working-directory: infra
        run: |
          git config user.name "palpitai-deploy[bot]"
          git config user.email "palpitai-deploy[bot]@users.noreply.github.com"
          git add k8s/apps/apps/palpitai/kustomization.yaml
          git commit -m "chore: palpitai image ${{ needs.build-push.outputs.sha_tag }}" || echo "nothing to commit"
          git push origin main
